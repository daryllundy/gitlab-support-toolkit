<!DOCTYPE html>
<html>
  <head>
    <title>GitLab Support Toolkit</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "GitLab Support Toolkit", root_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Dashboard", dashboard_path, class: "nav-link #{'active' if current_page?(dashboard_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Log Files", log_files_path, class: "nav-link #{'active' if current_page?(log_files_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Log Analysis", log_analyses_path, class: "nav-link #{'active' if current_page?(log_analyses_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "API Tests", api_tests_path, class: "nav-link #{'active' if current_page?(api_tests_path)}" %>
            </li>
          </ul>
          
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                Tools
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Upload Log File", new_log_file_path, class: "dropdown-item" %></li>
                <li><hr class="dropdown-divider"></li>
                <li><%= link_to "System Status", "#", class: "dropdown-item" %></li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= notice %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= alert %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="py-4">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-light text-center text-muted py-3 mt-5">
      <div class="container">
        <small>
          &copy; <%= Date.current.year %> GitLab Support Toolkit - 
          Built for GitLab Support Engineers
        </small>
      </div>
    </footer>
  </body>
</html>